console.log('this is an exercise in console logging');

const consoleValue = 'This is the value.'
console.log(consoleValue);

// const firstName = 'DMawi';
// const lastName = 'Asegid';
// console.log(firstName,lastName);

const firstName = 'John';
const lastName = 'Doe';
const city = 'Munich';
const occupation = 'Programmer';
const company = 'DCI';
console.log(firstName,lastName,occupation,city,company);

const profile = (firstName + ' ' + lastName + ' lives in ' + city + ' where he works as a ' + occupation + ' ' + 'for ' + company)

// const profile = `${firstName} ${lastName} lives in ${city} where he works as a ${occupation} for ${company}.`;

console.log(profile);
